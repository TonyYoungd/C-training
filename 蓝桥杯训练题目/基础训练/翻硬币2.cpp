#include"iostream"
#include"math.h" 
using namespace std;
//·­Ó²±Ò2
int main(){
	int n,m;
	int num;
	cin>>n>>m;
	cout<<sqrt(n)<<endl<<sqrt(m)<<endl; 
	num=(int)sqrt(n)*(int)sqrt(m);
	cout<<num;
	return 0;
} 
